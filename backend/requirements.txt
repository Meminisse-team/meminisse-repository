# Web framework
# [standard] extra가 fastapi-cli를 포함한다 — run-backend.bat이 `fastapi dev`
# CLI로 서버를 띄우므로 이것 없이는 새 가상환경에서 백엔드가 시작되지 않는다
# (RuntimeError: To use the fastapi command..., 2026-07-16 확인).
fastapi[standard]==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Database (Supabase PostgreSQL + pgvector)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# Config
# 2026-07-13: evals/ 폴더의 DeepEval 라벨추출 정확도 평가(README.md 2절)가
# deepeval>=4.1.0을 끌어오면서 pydantic-settings>=2.10.1, python-dotenv>=1.1.1을
# 요구해 이 두 핀도 함께 올렸다 — 앱 자체(FastAPI/설정 로딩) 동작에는 영향
# 없음을 전체 테스트 스위트로 확인했다. deepeval이 자체 pytest 플러그인을
# 자동 등록해 conftest.py의 GATEWAY_BACKEND=mock 기본값 강제보다 먼저 끼어드는
# 문제가 있었는데, 이건 버전이 아니라 그 플러그인 자체가 원인이라 pytest.ini에서
# `-p no:deepeval`로 비활성화해 해결했다(evals/ 스크립트에서는 라이브러리로만
# 쓰고 pytest 통합 기능은 쓰지 않으므로 비활성화해도 무방).
pydantic-settings==2.14.2
python-dotenv==1.2.2

# AI — Solar LLM + Upstage Embeddings (라벨 추출 / 산문 재조립 / 목차·챕터 집필 / RAG 임베딩)
# 둘 다 OpenAI SDK와 호환되므로 AsyncOpenAI(base_url="https://api.upstage.ai/v1")로 직통 호출
openai==1.54.0


# AI — Claude/Anthropic (자서전 집필 Phase 3/4의 세 번째 실험 프로바이더, app/clients/claude.py 참조)
anthropic==0.117.0

# AI — Upstage Document Parse (Phase 1 텍스트 사진 선(先) 라벨링)
# openai 패키지와 별개로 multipart/form-data 업로드가 필요하므로 httpx 직접 사용.
# app/clients/base.py의 get_upstage_client()가 AsyncOpenAI에 http_client를
# 직접 넘겨 깨진 내부 생성 경로 자체를 우회하도록 고쳐 고정을 풀었다(2026-07-20).
httpx>=0.28.1

# 비동기 워커 (세션 후처리 / Phase 3·4 파이프라인 / PDF 조판)
celery==5.4.0
redis==5.2.0

# AWS S3 (미디어 원본 저장)
boto3==1.35.74

# 인증 — 회원가입/로그인 자체는 Supabase Auth(GoTrue, httpx로 직접 호출)에 위임하므로
# 비밀번호 해싱 라이브러리는 불필요. 세션 토큰(JWT) 서명 검증에만 PyJWT를 쓴다
# (app/core/security.py, app/clients/supabase_auth.py). 이 Supabase 프로젝트는
# 세션 토큰을 ES256(비대칭키)로 서명하므로(2026-07-10 실연동 검증 중 확인) JWKS
# 기반 검증 경로가 필요하고, PyJWT의 ES256/RS256 지원은 cryptography 패키지가
# 있어야 동작한다(없으면 "ES256 requires 'cryptography' to be installed" 에러).
PyJWT==2.13.0
cryptography==49.0.0

# 실물 출판(Phase 5) — 최종 원고를 국판(A5) PDF로 조판한다(app/services/pdf_service.py).
# Jinja2로 고정 슬롯 템플릿에 콘텐츠를 주입하고, WeasyPrint의 CSS Paged Media로
# @page(도련·페이지번호)/orphans·widows(고아·과부 방지)/word-break: keep-all(한글 금칙)을
# 처리한다. pypdf는 조판 직후 QA(페이지 수·폰트 임베딩 확인)에만 쓴다.
# 주의: WeasyPrint는 pip 설치만으로 Windows에서 동작하지 않는다 — pango/gobject 등
# 네이티브 라이브러리가 필요해 GTK3 런타임을 별도로 설치해야 import 자체가 성공한다
# (winget install tschoonj.GTKForWindows, 2026-07-12 실제 설치·검증. 리눅스 배포
# 환경에서는 `apt install libpango-1.0-0 libpangoft2-1.0-0` 등으로 대체).
jinja2==3.1.6
weasyprint==69.0
pypdf==6.14.2

# Dev / test
pytest==8.3.4
pytest-asyncio==0.24.0

# P3 정량 평가체계(evals/, README.md 2·3절) — 라벨추출 정확도(슬롯 단위
# precision/recall)와 G-Eval 서사일관성을 Solar를 judge LLM으로 삼아 측정한다.
# 프로덕션 코드(app/)는 이 패키지에 의존하지 않는다 — evals/ 전용.
deepeval==4.1.0

# P3 비교 실험 설계(evals/baseline_ablation_comparison.py, README.md 6절) —
# 베이스라인/어블레이션 조건 간 대응표본 유의성 검정(Wilcoxon 부호순위검정).
# 프로덕션 코드(app/)는 이 패키지에 의존하지 않는다 — evals/ 전용.
scipy==1.17.1
